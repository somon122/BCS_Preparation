<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.world_tech_point.bcs_preparation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userProfileSection.LoginActivity"></activity>
        <activity android:name=".userProfileSection.ProfileSubmitActivity" />
        <activity android:name=".userProfileSection.DashboardActivity" />
        <activity android:name=".subjectivePracticeSection.SubjectDataShowActivity" />
        <activity android:name=".subjectivePracticeSection.SubjectListActivity" />
        <activity android:name=".splashScreen.OpenActivity" />
        <activity android:name=".commentSection.CommentsActivity" />
        <activity android:name=".videoSection.VideoPlayActivity" />
        <activity android:name=".videoSection.VideoShowActivity" />
        <activity android:name=".liveExamSection.LiveExamActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>